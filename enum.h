#if !defined(__ENUM_H)
#define  __ENUM_H

//ENUM'er:

enum kjonn {
	jente, gutt
};

enum resultatType {
	poeng, tid
};

enum typeTid {					// KANSKJE UBRUKELIG????
	tidel, hundredel, tusendel
};
#endif